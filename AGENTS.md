# Repository Guidelines

## プロジェクト構成

このリポジトリは、macOS向けの小規模なBashユーティリティです。`candle-timer.sh` にタイマー処理と音声通知のロジックがあります。`messages.conf.sample` は設定項目の見本です。実行前に、Git管理対象外の `messages.conf` へコピーしてください。`README.md` は利用者向けの導入手順、`LICENSE` はライセンスを記載しています。現在、テスト用・アセット用の専用ディレクトリはありません。スクリプトを分割する明確な必要性がない限り、処理は `candle-timer.sh` にまとめてください。

## ビルド・テスト・開発用コマンド

コンパイルや依存パッケージのインストールは不要です。

- `cp messages.conf.sample messages.conf`: ローカル設定ファイルを作成します。
- `./candle-timer.sh`: タイマーを起動します。停止するには `Ctrl+C` を押します。音声通知にmacOSの `say` コマンドを使用します。
- `bash -n candle-timer.sh`: 無限ループを実行せず、Bashの構文を検査します。
- `shellcheck candle-timer.sh`: ShellCheckが導入済みの場合に静的解析を実行します。

タイミングを変更した場合は、検証専用の短い設定を使い、各メッセージが期待した秒数で一度だけ再生されることを確認してください。

## コーディング規約と命名

Bashを対象とし、既存の2スペースインデントを維持します。変数展開とパスは引用符で囲み、コマンド置換には `$(...)` を使用してください。条件式は既存コードに合わせて `[` を優先します。設定値とメッセージは `CANDLE_DURATION_MIN`、`MSG_30_SEC` のような大文字のスネークケース、実行時のローカル変数は `now`、`mod` のような小文字を使用します。任意メッセージは、`say` の呼び出し前に `-n` で値の存在を確認してください。

## テスト方針

現時点では、自動テストやカバレッジ基準はありません。すべての変更で最低限 `bash -n candle-timer.sh` を通し、利用可能ならShellCheckも実行してください。挙動を変更した場合は、通知境界の秒数、設定ファイル欠落時のエラー、空の任意メッセージ、`Ctrl+C` による終了を手動確認します。自動テストを追加する場合は `test/` 以下に置き、検証する挙動が分かる名前を付けてください。

## コミットとプルリクエスト

最近のコミットは、`Enable customization` や `30秒前対応` のように短く要点を示しています。1コミットにつき1つの論理的変更に絞り、同様に簡潔な件名を使用してください。プルリクエストには、利用者への影響、実行した検証コマンド、手動確認の結果を記載します。設定キーを変更した場合は明記し、関連Issueがあればリンクしてください。スクリーンショットはドキュメントなどの視覚的な変更がある場合のみ添付します。
